#include <iostream>
using namespace std;

 void swap(int &a,int &b ){
    int temp;
    temp = b;
    b = a;
    a = temp;
}
int main() {
    int x=3,y=8;
    
    cout<<"x:"<<x<<"y:"<<y<<endl;;
    swap(x,y);
    cout<<"x:"<<x<<"y:"<<y<<endl;;
    return 0;
}
